import{_ as t,o as s,c as i,ag as a}from"./chunks/framework.DlJzNSsD.js";const g=JSON.parse('{"title":"Development guide","description":"","frontmatter":{},"headers":[],"relativePath":"development.md","filePath":"development.md","lastUpdated":1778273951000}'),o={name:"development.md"};function n(d,e,r,l,c,h){return s(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="development-guide" tabindex="-1">Development guide <a class="header-anchor" href="#development-guide" aria-label="Permalink to &quot;Development guide&quot;">​</a></h1><p>How to be productive in the Clawterm codebase. Aimed at new contributors and AI agents — designed so you can get to &quot;running dev build, tests passing, know where to look&quot; in about ten minutes.</p><blockquote><p>If you&#39;re trying to understand <em>what</em> the code does instead of <em>how to work on it</em>, start with <a href="./architecture"><code>architecture.md</code></a>.</p></blockquote><h2 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;Prerequisites&quot;">​</a></h2><ul><li><strong>Rust</strong> (stable) — <code>rustup install stable</code> if missing</li><li><strong>Node.js 18+</strong> (Node 24 is what CI uses)</li><li><strong>macOS</strong>: Xcode Command Line Tools (<code>xcode-select --install</code>)</li><li><strong>Linux</strong>: the <a href="https://v2.tauri.app/start/prerequisites/" target="_blank" rel="noreferrer">Tauri prerequisites</a> for your distro</li><li><strong>Windows</strong>: Visual Studio Build Tools with C++ workload</li></ul><p>Check everything works:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rustup</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> show</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">node</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --version</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --version</span></span></code></pre></div><h2 id="first-time-setup" tabindex="-1">First-time setup <a class="header-anchor" href="#first-time-setup" aria-label="Permalink to &quot;First-time setup&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clone</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://github.com/clawterm/clawterm.git</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clawterm</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span></code></pre></div><p><code>npm install</code> pulls JS dependencies and triggers the Tauri CLI install. The Rust toolchain doesn&#39;t fetch dependencies until the first <code>tauri dev</code> or <code>tauri build</code> run.</p><h2 id="running-the-dev-build" tabindex="-1">Running the dev build <a class="header-anchor" href="#running-the-dev-build" aria-label="Permalink to &quot;Running the dev build&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tauri</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span></span></code></pre></div><p>This:</p><ol><li>Starts the Vite dev server on <code>http://localhost:1420</code> (<code>vite.config.ts:16</code>, strict port — fails if 1420 is taken)</li><li>Compiles the Rust backend (<code>src-tauri/</code>) — first build is slow, subsequent rebuilds use <code>sccache</code> if installed</li><li>Launches the Clawterm desktop app pointing at the dev server</li></ol><p><strong>Hot reload behaviour</strong>:</p><ul><li>✅ <strong>TypeScript / CSS</strong> — instant HMR via Vite (port 1421 for HMR websocket if <code>TAURI_DEV_HOST</code> is set)</li><li>❌ <strong>Rust</strong> — no hot reload. After editing any file under <code>src-tauri/</code>, kill <code>npm run tauri dev</code> and re-run it. The Vite watcher explicitly ignores <code>**/src-tauri/**</code> (<code>vite.config.ts:27</code>) so frontend HMR isn&#39;t triggered by Rust edits.</li></ul><h2 id="tests" tabindex="-1">Tests <a class="header-anchor" href="#tests" aria-label="Permalink to &quot;Tests&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Command</th><th>What it runs</th><th>Where</th></tr></thead><tbody><tr><td><code>npm run test</code></td><td>Vitest, all <code>tests/**/*.test.ts</code></td><td><code>vitest.config.ts</code></td></tr><tr><td><code>npm run test:backend</code></td><td><code>cargo test</code> in <code>src-tauri/</code></td><td><code>src-tauri/src/**/mod tests</code></td></tr><tr><td><code>npm run lint</code></td><td>ESLint over <code>src/</code></td><td><code>eslint.config.js</code></td></tr><tr><td><code>npm run format:check</code></td><td>Prettier check (no writes)</td><td><code>package.json:27</code></td></tr><tr><td><code>npx tsc --noEmit</code></td><td>TypeScript typecheck</td><td><code>tsconfig.json</code></td></tr><tr><td><code>npm run preflight:frontend</code></td><td>Lint + format check + Vitest + tsc, <strong>all in parallel</strong></td><td><code>package.json:30</code></td></tr><tr><td><code>npm run preflight</code></td><td><code>preflight:frontend</code> then <code>cargo test</code></td><td><code>package.json:31</code></td></tr></tbody></table><p><code>npm run preflight</code> is the gate CI runs. If you run nothing else before pushing, run this. The frontend portion is parallelized so it completes in a few seconds even on a cold cache.</p><p>A single Vitest test file:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vitest</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tests/worktree-base.test.ts</span></span></code></pre></div><p>A single Rust test:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cargo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --manifest-path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src-tauri/Cargo.toml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> worktree::tests::is_inside_worktree</span></span></code></pre></div><h2 id="logging" tabindex="-1">Logging <a class="header-anchor" href="#logging" aria-label="Permalink to &quot;Logging&quot;">​</a></h2><p>The logger lives at <a href="../src/logger.ts"><code>src/logger.ts</code></a> and is <strong>console-only</strong> — there is no on-disk log file emitted by the app itself. Levels:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>debug → info → warn → error</span></span></code></pre></div><p>Default level is <code>debug</code>. Change at runtime from the DevTools console:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">logger.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setLevel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;info&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Logs are also kept in an in-memory <strong>circular buffer of the last 2000 entries</strong>, exposed via <code>logger.getBufferedLogs()</code> (returns the buffer as a single newline-joined string for export). This is what the &quot;Copy debug log&quot; command palette action uses.</p><p><strong>There are no log files on disk.</strong> If you need persistent logs across sessions, copy them out of the buffer manually before reload, or pipe <code>stdout</code>/<code>stderr</code> of <code>npm run tauri dev</code> to a file.</p><p>The Tauri webview also writes its own native logs to OS-standard locations:</p><table tabindex="0"><thead><tr><th>Platform</th><th>Webview log dir</th></tr></thead><tbody><tr><td>macOS</td><td><code>~/Library/Logs/com.clawterm.clawterm/</code></td></tr><tr><td>Windows</td><td><code>%LOCALAPPDATA%\\com.clawterm.clawterm\\logs\\</code></td></tr><tr><td>Linux</td><td><code>~/.local/share/com.clawterm.clawterm/logs/</code></td></tr></tbody></table><p>These are mostly empty unless something crashed at the Tauri/webview layer.</p><h2 id="devtools" tabindex="-1">DevTools <a class="header-anchor" href="#devtools" aria-label="Permalink to &quot;DevTools&quot;">​</a></h2><p>In dev builds (<code>npm run tauri dev</code>), the Tauri webview includes Chromium DevTools. To open them:</p><ul><li><strong>macOS</strong>: right-click → <strong>Inspect Element</strong></li><li><strong>Windows / Linux</strong>: F12 (default Chromium binding)</li></ul><p>DevTools are enabled by Tauri&#39;s default dev profile. There&#39;s no in-app keybinding to toggle them — modifying that would mean teaching the keybinding handler to call out to the Tauri webview API, which we haven&#39;t done.</p><p>In production builds DevTools are disabled. If you need to inspect a release build, build a <code>tauri dev</code> instead.</p><h2 id="performance-metrics" tabindex="-1">Performance metrics <a class="header-anchor" href="#performance-metrics" aria-label="Permalink to &quot;Performance metrics&quot;">​</a></h2><p><a href="../src/perf.ts"><code>src/perf.ts</code></a> defines a tiny <code>perfMetrics</code> instance that records <code>count</code>, <code>total</code>, <code>max</code>, and <code>last</code> for any labelled timing. Existing call sites:</p><ul><li><code>tab.show</code> — full show pipeline duration (<code>tab.ts:1318</code>)</li><li><code>renderTabList</code> — sidebar render (<code>terminal-manager.ts:1963</code>)</li><li>...and others scattered through the hot paths</li></ul><p>To view the current numbers:</p><ul><li><strong>Command palette</strong> → <strong>&quot;Show Performance Stats&quot;</strong> (logs to console)</li><li><strong>DevTools console</strong>: <code>perfMetrics.getSummary()</code></li><li><strong>Reset</strong>: <code>perfMetrics.reset()</code></li></ul><p>To instrument a new code path, use the helpers from <code>src/perf.ts</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { timed, timedAsync } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;./perf&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> timed</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;my.operation&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> doExpensiveThing</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> asyncResult</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> timedAsync</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;my.async-operation&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> fetchSomething</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span></code></pre></div><p>Or call <code>perfMetrics.record(&quot;label&quot;, durationMs)</code> directly if you have your own timing.</p><h2 id="reloading-config-without-restart" tabindex="-1">Reloading config without restart <a class="header-anchor" href="#reloading-config-without-restart" aria-label="Permalink to &quot;Reloading config without restart&quot;">​</a></h2><p><code>Cmd+Shift+R</code> (<code>Ctrl+Shift+R</code> on Windows/Linux) — bound to <code>reloadConfig</code> in <code>keybindings</code>. Calls <code>TerminalManager.reloadConfig()</code> which:</p><ol><li>Re-reads <code>~/.config/clawterm/config.json</code> from disk</li><li>Validates and falls back per-field on bad values</li><li>Re-applies CSS theme tokens</li><li>Calls <code>pane.applyConfig()</code> on every pane (font, cursor, scrollback, theme)</li><li>Restarts the central poll timer with the new interval</li><li>Re-renders the sidebar</li></ol><p>Takes ~50–200 ms total. Much faster than killing and restarting the dev build.</p><h2 id="where-things-live-on-disk" tabindex="-1">Where things live on disk <a class="header-anchor" href="#where-things-live-on-disk" aria-label="Permalink to &quot;Where things live on disk&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Thing</th><th>macOS / Linux</th><th>Windows</th></tr></thead><tbody><tr><td>Config</td><td><code>~/.config/clawterm/config.json</code></td><td><code>%APPDATA%\\clawterm\\config.json</code></td></tr><tr><td>Session</td><td><code>~/.config/clawterm/session.json</code></td><td><code>%APPDATA%\\clawterm\\session.json</code></td></tr><tr><td>Custom themes</td><td><code>~/.config/clawterm/themes/</code></td><td><code>%APPDATA%\\clawterm\\themes\\</code></td></tr><tr><td>Webview logs (Tauri)</td><td><code>~/Library/Logs/com.clawterm.clawterm/</code></td><td><code>%LOCALAPPDATA%\\com.clawterm.clawterm\\logs\\</code></td></tr></tbody></table><p>Resetting Clawterm completely:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># macOS / Linux</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -rf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.config/clawterm</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Windows</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Remove-Item</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -Recurse</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$env</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">:APPDATA\\clawterm&quot;</span></span></code></pre></div><p>A fresh <code>config.json</code> is written on next launch.</p><h2 id="build-artifacts" tabindex="-1">Build artifacts <a class="header-anchor" href="#build-artifacts" aria-label="Permalink to &quot;Build artifacts&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Build</th><th>Output</th></tr></thead><tbody><tr><td><code>npm run build</code> (frontend only)</td><td><code>dist/</code> (Vite bundle)</td></tr><tr><td><code>npm run tauri dev</code></td><td>Runs from <code>dist/</code> and <code>src-tauri/target/debug/</code></td></tr><tr><td><code>npm run tauri build</code></td><td>Bundled installer in <code>src-tauri/target/release/bundle/</code></td></tr></tbody></table><p>Per-platform release bundles:</p><table tabindex="0"><thead><tr><th>Platform</th><th>Path</th></tr></thead><tbody><tr><td>macOS</td><td><code>src-tauri/target/release/bundle/dmg/Clawterm_&lt;version&gt;_*.dmg</code></td></tr><tr><td>Windows</td><td><code>src-tauri/target/release/bundle/nsis/Clawterm_&lt;version&gt;_x64-setup.exe</code></td></tr><tr><td>Linux (deb)</td><td><code>src-tauri/target/release/bundle/deb/clawterm_&lt;version&gt;_amd64.deb</code></td></tr><tr><td>Linux (AppImage)</td><td><code>src-tauri/target/release/bundle/appimage/Clawterm_&lt;version&gt;_amd64.AppImage</code></td></tr></tbody></table><p>The Rust target dir is huge (~2 GB after a few builds). <code>cargo clean --manifest-path src-tauri/Cargo.toml</code> if you need the space.</p><h2 id="ci" tabindex="-1">CI <a class="header-anchor" href="#ci" aria-label="Permalink to &quot;CI&quot;">​</a></h2><p>Workflows live in <code>.github/workflows/</code>:</p><ul><li><strong><code>ci.yml</code></strong> — runs on every push and PR. Frontend job (Ubuntu, Node 24): <code>npm run preflight:frontend</code>. Backend job (macOS, Windows, Linux): <code>cargo clippy -- -D warnings</code> + <code>cargo test</code>. Plus a version-sync check that asserts <code>package.json</code>, <code>tauri.conf.json</code>, and <code>Cargo.toml</code> agree.</li><li><strong><code>release.yml</code></strong> — triggered by pushing a <code>vX.Y.Z</code> tag. Builds and publishes the GitHub Release with macOS / Windows / Linux artifacts. See <a href="./../RELEASING"><code>RELEASING.md</code></a> for the full release workflow.</li></ul><p>There are no pre-commit hooks installed by <code>npm install</code>. CI is the only gate. Run <code>npm run preflight</code> locally before pushing if you want fast feedback.</p><h2 id="state-inspection-from-devtools" tabindex="-1">State inspection from DevTools <a class="header-anchor" href="#state-inspection-from-devtools" aria-label="Permalink to &quot;State inspection from DevTools&quot;">​</a></h2><p><code>TerminalManager</code> does <strong>not</strong> expose itself on <code>window</code>, so there&#39;s no <code>window.__clawterm</code> or similar global. If you need to poke at runtime state interactively:</p><ol><li>Open DevTools (<code>Inspect Element</code> in dev)</li><li>Set a breakpoint in <code>src/main.ts:13</code> (right after <code>manager.init()</code>)</li><li>From the paused state, walk the closure or temporarily expose <code>manager</code> to a global:</li></ol><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// src/main.ts — temporary debug only, do NOT commit</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> manager</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TerminalManager</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(window </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> any</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).__clawterm </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> manager;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> manager.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p>Then in DevTools:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">__clawterm.tabs                 </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Map&lt;string, Tab&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">__clawterm.activeTabId</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">__clawterm.tabs.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">values</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()][</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">].panes[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">].state</span></span></code></pre></div><p>Don&#39;t commit the <code>window</code> assignment. We deliberately don&#39;t expose it in shipped builds.</p><h2 id="common-gotchas" tabindex="-1">Common gotchas <a class="header-anchor" href="#common-gotchas" aria-label="Permalink to &quot;Common gotchas&quot;">​</a></h2><p>These are the load-bearing invariants and surprising patterns most likely to bite a contributor.</p><h3 id="_1-the-2-frame-show-hide-raf-chain-in-tab-ts-1261-1349-is-performance-critical" tabindex="-1">1. The 2-frame show/hide rAF chain in <code>tab.ts:1261-1349</code> is performance-critical <a class="header-anchor" href="#_1-the-2-frame-show-hide-raf-chain-in-tab-ts-1261-1349-is-performance-critical" aria-label="Permalink to &quot;1. The 2-frame show/hide rAF chain in \`tab.ts:1261-1349\` is performance-critical&quot;">​</a></h3><p>This pipeline took <strong>eleven</strong> issues (#167, #182, #184, #227, #305, #419, plus earlier ones) to stabilize. Adding a third frame is a 30%+ tab-switch latency regression. Don&#39;t add work to it without folding it into Frame 1 (destabilizing operations) or Frame 2 (single authoritative restore). Read <a href="./architecture#the-showhide-pipeline">the architecture doc&#39;s show/hide section</a> before changing it.</p><h3 id="_2-the-scroll-lock-locks-distance-from-bottom-not-viewporty" tabindex="-1">2. The scroll lock locks distance-from-bottom, not viewportY <a class="header-anchor" href="#_2-the-scroll-lock-locks-distance-from-bottom-not-viewporty" aria-label="Permalink to &quot;2. The scroll lock locks distance-from-bottom, not viewportY&quot;">​</a></h3><p><code>pane.lockScroll()</code> (<code>pane.ts:1081</code>) saves <code>lockedDistanceFromBottom</code>, <strong>not</strong> an absolute position index. This is because the hidden-tab scrollback trim from #305 mutates the buffer underneath the lock, invalidating any saved index. Distance from the bottom survives because xterm only ever trims the front of the scrollback. There&#39;s a tripwire in <code>unlockScroll()</code> that fires <code>logger.warn</code> if the buffer length changed unexpectedly during the lock window — gated via <code>trimmedDuringHide</code> so it doesn&#39;t fire on the legitimate #305 path.</p><h3 id="_3-polling-is-centralized" tabindex="-1">3. Polling is centralized <a class="header-anchor" href="#_3-polling-is-centralized" aria-label="Permalink to &quot;3. Polling is centralized&quot;">​</a></h3><p>All process-info polling goes through <code>TerminalManager.startCentralPoll()</code>. Don&#39;t add a per-tab <code>setInterval</code>. If you need to poll new state, add a field to the <code>poll_pane_info</code> Rust handler and consume it in <code>Tab.pollProcessInfo()</code>. Adding a parallel polling loop will silently double IPC pressure.</p><h3 id="_4-the-webgl-pool-s-eviction-order-matters-pane-webgl-ts" tabindex="-1">4. The WebGL pool&#39;s eviction order matters (<code>pane-webgl.ts</code>) <a class="header-anchor" href="#_4-the-webgl-pool-s-eviction-order-matters-pane-webgl-ts" aria-label="Permalink to &quot;4. The WebGL pool&#39;s eviction order matters (\`pane-webgl.ts\`)&quot;">​</a></h3><p>The shared LRU pool of WebGL contexts (cap: 6) requires that <code>pool.shift()</code> removes the victim <strong>before</strong> calling <code>victim.deactivate()</code>, otherwise the victim&#39;s own <code>pool.remove()</code> call inside <code>deactivate()</code> becomes a silent no-op. Easy ordering bug. Look for the <code>IMPORTANT</code> comment.</p><h3 id="_5-project-switching-keybindings-are-checked-before-tab-switching-keybindings" tabindex="-1">5. Project-switching keybindings are checked <strong>before</strong> tab-switching keybindings <a class="header-anchor" href="#_5-project-switching-keybindings-are-checked-before-tab-switching-keybindings" aria-label="Permalink to &quot;5. Project-switching keybindings are checked **before** tab-switching keybindings&quot;">​</a></h3><p>If a user binds the same key to both <code>nextTab</code> and <code>nextProject</code> (or accidentally collides them), the project action wins. This is intentional (#410) but surprising — if a tab keybinding &quot;doesn&#39;t fire,&quot; check whether a project binding is shadowing it.</p><h3 id="_6-pty-env-var-setup-strips-claudecode" tabindex="-1">6. PTY env var setup strips <code>CLAUDECODE</code> <a class="header-anchor" href="#_6-pty-env-var-setup-strips-claudecode" aria-label="Permalink to &quot;6. PTY env var setup strips \`CLAUDECODE\`&quot;">​</a></h3><p><code>src-tauri/src/main.rs</code> removes <code>CLAUDECODE</code> from the spawn environment to prevent nested Claude Code invocation issues, and sets <code>TERM</code> / <code>COLORTERM</code> / <code>TERM_PROGRAM</code> for color rendering and agent detection in child shells. If you change PTY env handling, don&#39;t blindly inherit the parent env.</p><h3 id="_7-defaults-live-in-config-ts-schemas-in-config-types-ts" tabindex="-1">7. Defaults live in <code>config.ts</code>, schemas in <code>config-types.ts</code> <a class="header-anchor" href="#_7-defaults-live-in-config-ts-schemas-in-config-types-ts" aria-label="Permalink to &quot;7. Defaults live in \`config.ts\`, schemas in \`config-types.ts\`&quot;">​</a></h3><p><code>validateConfig()</code> falls back per-field, never refuses to start because of a bad config. Don&#39;t hardcode defaults anywhere else, and don&#39;t add validation that throws — the contract is &quot;log + replace bad fields.&quot; The full schema is documented in <a href="./reference/configuration"><code>docs/reference/configuration.md</code></a>.</p><h3 id="_8-session-format-is-versioned" tabindex="-1">8. Session format is versioned <a class="header-anchor" href="#_8-session-format-is-versioned" aria-label="Permalink to &quot;8. Session format is versioned&quot;">​</a></h3><p><code>session.json</code> is V2: <code>{ version, projects, activeProject }</code>. V1 (<code>{ tabs, activeIndex }</code>) is auto-wrapped on load. If you add fields to the session shape, decide whether you need to bump to V3 and add a normalizer in <code>loadSession()</code> so old session files keep loading. Breaking session compatibility silently means users lose their tab layouts on upgrade.</p><h2 id="editor-lsp-tips" tabindex="-1">Editor / LSP tips <a class="header-anchor" href="#editor-lsp-tips" aria-label="Permalink to &quot;Editor / LSP tips&quot;">​</a></h2><ul><li><strong>VS Code</strong>: install <code>rust-analyzer</code> and <code>Tauri</code> extensions. The TypeScript LSP picks up <code>tsconfig.json</code> from the repo root automatically.</li><li><strong>JetBrains</strong>: open the repo as a single project; both the TypeScript and Rust IDEs work side by side with no extra config.</li><li><strong>Format on save</strong>: Prettier handles TypeScript/CSS, <code>rustfmt</code> handles Rust. The repo&#39;s preflight check runs <code>prettier --check</code>, so set your editor to format on save and <code>npm run preflight</code> will pass without manual fixup.</li></ul><h2 id="ten-minute-onboarding-checklist" tabindex="-1">Ten-minute onboarding checklist <a class="header-anchor" href="#ten-minute-onboarding-checklist" aria-label="Permalink to &quot;Ten-minute onboarding checklist&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[ ] Install Rust + Node 18+</span></span>
<span class="line"><span>[ ] git clone &amp;&amp; cd clawterm</span></span>
<span class="line"><span>[ ] npm install</span></span>
<span class="line"><span>[ ] npm run tauri dev          ← takes a few minutes the first time (Rust build)</span></span>
<span class="line"><span>[ ] Right-click in the app window → Inspect Element to verify DevTools</span></span>
<span class="line"><span>[ ] Open command palette (Cmd+Shift+P) → &quot;Show Performance Stats&quot; to verify metrics</span></span>
<span class="line"><span>[ ] In a separate terminal: npm run preflight  (must pass before push)</span></span>
<span class="line"><span>[ ] Read docs/architecture.md</span></span>
<span class="line"><span>[ ] Read docs/features/worktrees.md if you&#39;re touching worktree code</span></span>
<span class="line"><span>[ ] Look up the file you&#39;re editing in architecture.md&#39;s &quot;Where to find things&quot; table</span></span></code></pre></div><p>If anything in this guide is wrong or missing, fix it and ship the fix. This file exists to make the next contributor&#39;s first day shorter than yours.</p>`,94)])])}const u=t(o,[["render",n]]);export{g as __pageData,u as default};
