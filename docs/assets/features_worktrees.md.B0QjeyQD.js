import{_ as t,o,c as r,ag as a}from"./chunks/framework.DlJzNSsD.js";const u=JSON.parse('{"title":"Worktrees","description":"","frontmatter":{},"headers":[],"relativePath":"features/worktrees.md","filePath":"features/worktrees.md","lastUpdated":1778271662000}'),s={name:"features/worktrees.md"};function i(n,e,d,c,l,h){return o(),r("div",null,[...e[0]||(e[0]=[a(`<h1 id="worktrees" tabindex="-1">Worktrees <a class="header-anchor" href="#worktrees" aria-label="Permalink to &quot;Worktrees&quot;">​</a></h1><p>Clawterm uses <a href="https://git-scm.com/docs/git-worktree" target="_blank" rel="noreferrer">git worktrees</a> to give each agent its own working directory and branch on the same repository, without the cost of a fresh clone or the friction of <code>git stash</code>/<code>git checkout</code>. This page is the single source of truth for how worktrees work in the app.</p><blockquote><p><strong>TL;DR</strong> — <code>Cmd+Shift+N</code> opens the new-worktree dialog, picks (or creates) a branch, and opens a tab in <code>&lt;parent-of-repo&gt;/.clawterm-worktrees/&lt;repo-name&gt;/&lt;branch&gt;/</code>. The location is configurable via <code>worktree.directory</code>. Existing worktrees from older installs continue to work unchanged.</p></blockquote><h2 id="why-worktrees" tabindex="-1">Why worktrees <a class="header-anchor" href="#why-worktrees" aria-label="Permalink to &quot;Why worktrees&quot;">​</a></h2><p>Running multiple coding agents against the same repo means each one needs:</p><ul><li><strong>Its own branch</strong> — agents shouldn&#39;t fight over a single working tree</li><li><strong>Its own filesystem state</strong> — file edits, build artifacts, dependency installs all need to be isolated</li><li><strong>Fast startup</strong> — fresh <code>git clone</code> is too slow to do interactively per agent</li></ul><p><code>git worktree</code> is the right primitive: it creates a second working directory backed by the same <code>.git</code>, so branches and history are shared but file state is isolated. Clawterm wraps the lifecycle (create / lock / unlock / remove) and resolves the location automatically.</p><h2 id="where-worktrees-live" tabindex="-1">Where worktrees live <a class="header-anchor" href="#where-worktrees-live" aria-label="Permalink to &quot;Where worktrees live&quot;">​</a></h2><p>The default since <a href="https://github.com/clawterm/clawterm/issues/416" target="_blank" rel="noreferrer">#416</a> is <strong>sibling-of-repo, namespaced by repo name</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>~/Code/</span></span>
<span class="line"><span>├── myrepo/                              ← your main repo (working tree untouched)</span></span>
<span class="line"><span>│   └── .git/</span></span>
<span class="line"><span>└── .clawterm-worktrees/                 ← sibling, hidden, namespaced</span></span>
<span class="line"><span>    └── myrepo/</span></span>
<span class="line"><span>        ├── feature-x-wt-1/              ← actual worktrees</span></span>
<span class="line"><span>        └── feature-y-wt-2/</span></span></code></pre></div><p>This isolation is <strong>not cosmetic</strong>. The previous default put <code>.clawterm-worktrees/</code> <em>inside</em> the main repo, which broke every tool that walks the repo from the root: Biome, Vitest, tsc, ESLint, Prettier all discovered the worktrees&#39; copies of <code>biome.jsonc</code>, <code>vitest.config.mts</code>, etc., and either crashed pre-commit hooks or doubled-ran tests against the wrong tree. The sibling layout makes the worktrees structurally invisible to anything invoked from the main repo. Full background in <a href="https://github.com/clawterm/clawterm/issues/415" target="_blank" rel="noreferrer">#415</a>.</p><h3 id="the-three-resolver-modes" tabindex="-1">The three resolver modes <a class="header-anchor" href="#the-three-resolver-modes" aria-label="Permalink to &quot;The three resolver modes&quot;">​</a></h3><p><code>worktree.directory</code> in <code>~/.config/clawterm/config.json</code> is interpreted by <a href="../../src/worktree-base.ts"><code>src/worktree-base.ts</code></a> at worktree-creation time. The string&#39;s shape selects one of three modes:</p><table tabindex="0"><thead><tr><th>Mode</th><th>Trigger</th><th>Resolves to</th><th>Use case</th></tr></thead><tbody><tr><td><strong>Auto</strong> (default)</td><td><code>&quot;&quot;</code></td><td><code>&lt;parent-of-repo&gt;/.clawterm-worktrees/&lt;repo-name&gt;/</code></td><td>Most users — just works</td></tr><tr><td><strong>Absolute</strong></td><td><code>&quot;/abs/path&quot;</code></td><td><code>&lt;abs-path&gt;/&lt;repo-name&gt;/</code></td><td>Central worktree cache shared across repos</td></tr><tr><td><strong>Tilde</strong></td><td><code>&quot;~&quot;</code> or <code>&quot;~/foo&quot;</code></td><td><code>&lt;expanded&gt;/&lt;repo-name&gt;/</code></td><td>Same as absolute, with <code>$HOME</code> expansion</td></tr><tr><td><strong>Legacy in-repo</strong></td><td><code>&quot;foo&quot;</code> or <code>&quot;.foo&quot;</code></td><td><code>&lt;repo-root&gt;/foo/</code></td><td><strong>Opt-in only</strong> — breaks parent-repo tooling, see #415</td></tr></tbody></table><p><strong>Auto mode fallbacks</strong> (rare): if the repo is at the filesystem root (<code>/repo</code> — no parent dir to write to) or the parent isn&#39;t writable, the resolver falls back to <code>~/.clawterm-worktrees/&lt;repo-name&gt;/</code>. The parent-writable check is a no-op in production (returns true) and only the parent-empty case actually triggers the fallback; tests inject the probe via <code>ResolveWorktreeBaseOptions.isWritable</code>.</p><p><strong>Tilde edge case</strong>: only <code>&quot;~&quot;</code> and <code>&quot;~/foo&quot;</code> are recognized as home expansions. POSIX <code>~user</code> shorthand (another user&#39;s home) is <strong>not</strong> supported — it falls through to legacy mode so the literal <code>~user</code> appears under the repo root and the misconfiguration is visible. See <a href="../../tests/worktree-base.test.ts">test cases</a>.</p><h2 id="creating-a-worktree" tabindex="-1">Creating a worktree <a class="header-anchor" href="#creating-a-worktree" aria-label="Permalink to &quot;Creating a worktree&quot;">​</a></h2><p>Two entry points, both defined in <code>src/worktree-actions.ts</code>:</p><h3 id="cmd-shift-n-—-new-worktree-tab" tabindex="-1"><code>Cmd+Shift+N</code> — new worktree tab <a class="header-anchor" href="#cmd-shift-n-—-new-worktree-tab" aria-label="Permalink to &quot;\`Cmd+Shift+N\` — new worktree tab&quot;">​</a></h3><p>Keybinding: <code>newWorktreeTab</code> (configurable in <code>keybindings.newWorktreeTab</code>). Opens <a href="../../src/worktree-dialog.ts"><code>showWorktreeDialog</code></a> with:</p><ul><li><strong>Branch picker</strong> — search-as-you-type over all local + remote branches, with <code>• has worktree</code> annotations on branches that already have one</li><li><strong>Create-new-branch panel</strong> — appears when you type a name that doesn&#39;t match any existing branch; lets you pick a base branch (defaults to <code>main</code> or <code>master</code>)</li><li><strong>Agent launcher</strong> — optional command to run automatically in the new tab once the worktree is ready (e.g. <code>claude</code>); defaults to <code>worktree.defaultAgent</code></li><li><strong>Cancel / Create Tab</strong> buttons</li></ul><p>On submit, <code>createAgentTab()</code> runs:</p><ol><li><code>invoke(&quot;create_worktree&quot;, ...)</code> — Rust does <code>git worktree add</code> (see below)</li><li><code>invoke(&quot;lock_worktree&quot;, ...)</code> — locks the worktree against accidental <code>git worktree remove</code></li><li><code>ctx.createTab(worktreeDir)</code> — opens a new tab with the worktree as cwd</li><li>Stores <code>worktreePath</code> and <code>repoRoot</code> on the tab and its initial pane</li><li>Runs <code>worktree.postCreateHooks</code> in order, with 500 ms gaps</li><li>Optionally runs the agent launcher command after another 300 ms</li></ol><h3 id="cmd-d-cmd-shift-d-→-split-to-branch" tabindex="-1"><code>Cmd+D</code> / <code>Cmd+Shift+D</code> → split-to-branch <a class="header-anchor" href="#cmd-d-cmd-shift-d-→-split-to-branch" aria-label="Permalink to &quot;\`Cmd+D\` / \`Cmd+Shift+D\` → split-to-branch&quot;">​</a></h3><p>The split shortcuts call <code>splitWithChoice()</code> which detects whether the focused pane&#39;s cwd is in a git repo. If yes, you get a small choice dialog: <strong>same branch</strong> (regular split, both panes share state) or <strong>new worktree</strong> (split into a freshly-created worktree on a new branch).</p><p>The &quot;new worktree&quot; path runs <code>openSplitToBranchDialog()</code>, which:</p><ol><li>Detects the current branch via <code>get_git_branch</code></li><li>Strips any <code>-wt-N</code> suffix to prevent stacking like <code>main-wt-1-wt-1</code> (#351)</li><li>Generates a unique branch name <code>&lt;root&gt;-wt-1</code>, <code>&lt;root&gt;-wt-2</code>, etc., by checking against <code>list_branches</code></li><li>Resolves the worktree base via <code>resolveWorktreeBase()</code></li><li>Calls <code>splitToBranch()</code> which runs the same Rust IPC sequence as above and then splits the focused pane into the new worktree</li></ol><p>Command palette also exposes both flows: <strong>&quot;New Agent Tab on Branch&quot;</strong>, <strong>&quot;Split Right → Worktree&quot;</strong>, <strong>&quot;Split Down → Worktree&quot;</strong>.</p><h2 id="what-create-worktree-does" tabindex="-1">What <code>create_worktree</code> does <a class="header-anchor" href="#what-create-worktree-does" aria-label="Permalink to &quot;What \`create_worktree\` does&quot;">​</a></h2><p><a href="./../../src-tauri/src/worktree.rs"><code>src-tauri/src/worktree.rs:107</code></a> is the Rust handler. It:</p><ol><li><strong>Refuses to nest</strong> — calls <code>is_inside_worktree(repo_dir)</code> which compares <code>git rev-parse --git-dir</code> against <code>--git-common-dir</code> (combined into a single subprocess call). If they differ, the caller is inside a worktree, and creation is rejected with a clear error: <em>&quot;Refusing to create a worktree from inside another worktree. Open a tab in the main repository first.&quot;</em> This is defense in depth on top of <a href="https://github.com/clawterm/clawterm/issues/351" target="_blank" rel="noreferrer">#351</a>&#39;s <code>find_repo_root</code> fix.</li><li><strong>Creates the parent directory</strong> — <code>std::fs::create_dir_all()</code> for whatever directory will hold the new worktree</li><li><strong>Runs <code>git worktree add</code></strong> — with <code>-b &lt;branch&gt;</code> if <code>createBranch</code> is true, otherwise checking out an existing branch</li><li><strong>Returns the worktree path</strong> on success, or git&#39;s stderr on failure</li></ol><p>The frontend calls this through <code>invokeWithTimeout(&quot;create_worktree&quot;, ..., 10000)</code> so the IPC has a 10 s upper bound.</p><h2 id="locking" tabindex="-1">Locking <a class="header-anchor" href="#locking" aria-label="Permalink to &quot;Locking&quot;">​</a></h2><p>After creation succeeds, the frontend immediately calls <code>invoke(&quot;lock_worktree&quot;, ...)</code>. This runs <code>git worktree lock --reason &quot;In use by ClawTerm&quot;</code> on the new worktree. The lock prevents <code>git worktree remove</code> (without <code>--force</code>) from succeeding — protection against an agent or script in another pane accidentally deleting the worktree someone else is editing.</p><p>The lock is <strong>released</strong> in three places:</p><ul><li><strong>Tab close with <code>autoCleanup: true</code></strong> — <code>unlockAndRemoveWorktree()</code> unlocks then removes</li><li><strong>Tab close with <code>autoCleanup: false</code></strong> (default) — <code>unlockWorktree()</code> unlocks but leaves the worktree on disk for manual cleanup later</li><li><strong>Split-to-branch failure rollback</strong> — if creating the worktree succeeded but the actual split failed (pane limit, PTY error), the worktree is unlocked and force-removed in <code>splitToBranch()</code>&#39;s cleanup branch</li></ul><p><code>lock_worktree</code> and <code>unlock_worktree</code> both treat &quot;already locked&quot; / &quot;not locked&quot; as benign no-ops, so re-running them is safe.</p><h2 id="tab-close-lifecycle" tabindex="-1">Tab close lifecycle <a class="header-anchor" href="#tab-close-lifecycle" aria-label="Permalink to &quot;Tab close lifecycle&quot;">​</a></h2><p>When a tab containing a worktree pane is closed (<code>Tab.onPaneClose</code> callback in <code>terminal-manager.ts</code>):</p><ol><li><strong>Sibling check</strong> — does another pane in the same tab still use this worktree? If yes, do nothing.</li><li><strong>Cross-tab check</strong> — does another tab still use this worktree (<code>isWorktreeInUse()</code> in <code>worktree.rs</code>)? If yes, do nothing.</li><li><strong><code>autoCleanup</code> check</strong> — if the config flag is <code>true</code>, run <code>unlockAndRemoveWorktree()</code>. If <code>false</code> (default), just <code>unlockWorktree()</code> so the directory survives for manual cleanup.</li></ol><p><code>autoCleanup</code> defaults to <code>false</code> because deleting an active worktree is a destructive operation Clawterm can&#39;t always tell apart from &quot;the user is just closing the tab to free a slot in the sidebar.&quot; Set it to <code>true</code> only if your workflow is &quot;agent finishes → I&#39;m done with this branch.&quot;</p><h2 id="persistence" tabindex="-1">Persistence <a class="header-anchor" href="#persistence" aria-label="Permalink to &quot;Persistence&quot;">​</a></h2><p>Worktree metadata is persisted on a per-tab and per-pane basis in <a href="../../src/session.ts"><code>session.json</code></a>:</p><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// SessionTab</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;title&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;feature-auth&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;cwd&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/Users/me/Code/.clawterm-worktrees/myrepo/feature-auth-wt-1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;worktreePath&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/Users/me/Code/.clawterm-worktrees/myrepo/feature-auth-wt-1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;repoRoot&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/Users/me/Code/myrepo&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The same fields exist on <code>SessionSplitLeaf</code> for split panes that own a worktree independently.</p><p>On session restore (<code>restoreOneTab()</code> in <code>terminal-manager.ts</code>), Clawterm:</p><ol><li>Validates that the saved cwd still exists (<code>validate_dir</code>); falls back to <code>$HOME</code> if not</li><li>Re-creates the tab in that cwd</li><li>Re-attaches the saved <code>worktreePath</code> and <code>repoRoot</code> to the tab and its initial pane</li></ol><p>The resolver does <strong>not</strong> run on restore — it only runs at <em>creation</em> time. This means existing worktrees from older Clawterm versions (when the default was <code>.clawterm-worktrees</code> inside the repo) continue to work indefinitely. The next worktree you create after upgrading uses the new sibling layout, but the old ones stay where they are.</p><h2 id="the-legacy-migration-hint" tabindex="-1">The legacy migration hint <a class="header-anchor" href="#the-legacy-migration-hint" aria-label="Permalink to &quot;The legacy migration hint&quot;">​</a></h2><p><code>maybeShowLegacyWorktreeHint()</code> in <a href="../../src/terminal-manager.ts"><code>terminal-manager.ts</code></a> is a one-time non-blocking startup check for users upgrading from the pre-#416 default. It runs only if:</p><ul><li><code>worktree.directory === &quot;&quot;</code> (the new auto mode)</li><li>The localStorage flag <code>clawterm-legacy-worktree-hint-shown</code> is not set</li><li>The active tab has resolved its cwd (waits 2.5 s for the first poll cycle, falls back to the constructor cwd if polling hasn&#39;t run yet)</li><li>The cwd is in a git repository</li><li>That repository has a non-empty <code>.clawterm-worktrees/</code> directory at its root (Rust check: <code>has_legacy_in_repo_worktrees</code>)</li></ul><p>If all conditions match, it shows an 8-second info toast:</p><blockquote><p>Found old worktrees in <code>.clawterm-worktrees/</code>. New worktrees will now be created outside the repo (#415).</p></blockquote><p>The localStorage flag is set after the first time the toast fires (regardless of how many repos have legacy worktrees), so the user sees it at most once across all their repos. Existing worktrees keep working — the hint is purely informational, no migration is performed.</p><h2 id="failure-modes" tabindex="-1">Failure modes <a class="header-anchor" href="#failure-modes" aria-label="Permalink to &quot;Failure modes&quot;">​</a></h2><table tabindex="0"><thead><tr><th>What can go wrong</th><th>What happens</th><th>Where</th></tr></thead><tbody><tr><td>Branch already exists when <code>createBranch: true</code></td><td><code>git worktree add</code> errors; toast shows the stderr verbatim</td><td><code>worktree-actions.ts:108</code></td></tr><tr><td>Worktree directory already exists on disk</td><td>Same — <code>git worktree add</code> errors</td><td>Same</td></tr><tr><td>Repository at filesystem root (<code>/repo</code>)</td><td>Resolver falls back to <code>~/.clawterm-worktrees/&lt;repo-name&gt;/</code></td><td><code>worktree-base.ts:71</code></td></tr><tr><td>Caller is inside another worktree</td><td>Rust refuses with clear error message</td><td><code>worktree.rs:120</code></td></tr><tr><td>Split succeeds but pane limit hit, no PTY slot</td><td>Frontend detects via pane-count comparison, unlocks + force-removes the orphaned worktree</td><td><code>worktree-actions.ts:213</code></td></tr><tr><td>User cancels the dialog mid-flow</td><td><code>onResult</code> callback never fires; nothing is created</td><td><code>worktree-dialog.ts</code></td></tr><tr><td><code>lock_worktree</code> fails (e.g., already locked)</td><td>Treated as benign; debug log only, creation continues</td><td><code>worktree-actions.ts:79</code></td></tr><tr><td>Tab close with <code>autoCleanup: true</code> but worktree has uncommitted changes</td><td><code>git worktree remove</code> errors; debug log; the directory stays</td><td><code>terminal-manager.ts:733</code></td></tr></tbody></table><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><p>Defaults live in <a href="../../src/config.ts"><code>src/config.ts:122</code></a>, schema in <a href="../../src/config-types.ts"><code>src/config-types.ts:74</code></a>. All keys live under <code>worktree</code>:</p><table tabindex="0"><thead><tr><th>Key</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>directory</code></td><td><code>string</code></td><td><code>&quot;&quot;</code></td><td>Resolver mode selector. See <a href="#the-three-resolver-modes">the three modes</a>.</td></tr><tr><td><code>postCreateHooks</code></td><td><code>string[]</code></td><td><code>[]</code></td><td>Shell commands to run in the new worktree after creation, in order. 500 ms gap between each. Use this for <code>npm install</code>, <code>mise install</code>, etc.</td></tr><tr><td><code>autoCleanup</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Run <code>git worktree remove</code> automatically when the last tab/pane using a worktree closes.</td></tr><tr><td><code>defaultAgent</code></td><td><code>string</code></td><td><code>&quot;&quot;</code></td><td>Pre-fills the dialog&#39;s &quot;agent launcher&quot; input. Set to <code>&quot;claude&quot;</code> if you want every new worktree to start Claude Code automatically.</td></tr></tbody></table><p>Keybinding (under <code>keybindings</code>):</p><table tabindex="0"><thead><tr><th>Key</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>newWorktreeTab</code></td><td><code>Cmd+Shift+N</code> (macOS), <code>Ctrl+Shift+N</code> (Windows/Linux)</td><td>Open the new-worktree dialog</td></tr></tbody></table><h2 id="common-workflows" tabindex="-1">Common workflows <a class="header-anchor" href="#common-workflows" aria-label="Permalink to &quot;Common workflows&quot;">​</a></h2><h3 id="i-m-starting-a-new-agent-task-—-give-me-a-fresh-branch" tabindex="-1">&quot;I&#39;m starting a new agent task — give me a fresh branch&quot; <a class="header-anchor" href="#i-m-starting-a-new-agent-task-—-give-me-a-fresh-branch" aria-label="Permalink to &quot;&quot;I&#39;m starting a new agent task — give me a fresh branch&quot;&quot;">​</a></h3><p><code>Cmd+Shift+N</code> → type a new branch name → pick base branch (usually <code>main</code>) → optionally type <code>claude</code> in the agent field → Enter. Clawterm creates the worktree, opens a tab in it, and runs <code>claude</code> for you.</p><h3 id="i-want-two-agents-on-the-same-task-comparing-approaches" tabindex="-1">&quot;I want two agents on the same task, comparing approaches&quot; <a class="header-anchor" href="#i-want-two-agents-on-the-same-task-comparing-approaches" aria-label="Permalink to &quot;&quot;I want two agents on the same task, comparing approaches&quot;&quot;">​</a></h3><p><code>Cmd+D</code> to split → pick <strong>new worktree</strong> → confirm. The split pane gets <code>&lt;branch&gt;-wt-1</code>. Repeat with another <code>Cmd+D</code> for <code>-wt-2</code>. Both agents work in isolation.</p><h3 id="i-want-a-central-worktree-cache-so-all-my-repos-share-one-location" tabindex="-1">&quot;I want a central worktree cache so all my repos share one location&quot; <a class="header-anchor" href="#i-want-a-central-worktree-cache-so-all-my-repos-share-one-location" aria-label="Permalink to &quot;&quot;I want a central worktree cache so all my repos share one location&quot;&quot;">​</a></h3><p>Set in <code>config.json</code>:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;worktree&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;directory&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;~/.cache/clawterm-worktrees&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Worktrees from <code>myrepo</code> land in <code>~/.cache/clawterm-worktrees/myrepo/</code>, worktrees from <code>otherrepo</code> in <code>~/.cache/clawterm-worktrees/otherrepo/</code>, etc. Repo-name namespacing prevents collisions when two different parent directories contain repos with the same name.</p><h3 id="i-want-the-old-in-repo-behaviour-back" tabindex="-1">&quot;I want the old in-repo behaviour back&quot; <a class="header-anchor" href="#i-want-the-old-in-repo-behaviour-back" aria-label="Permalink to &quot;&quot;I want the old in-repo behaviour back&quot;&quot;">​</a></h3><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;worktree&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;directory&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;.clawterm-worktrees&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>This goes through the <strong>legacy</strong> branch of the resolver. Be aware: tools that walk the repo will now discover the worktrees&#39; nested config files. Don&#39;t do this unless you have a specific reason (shared <code>node_modules</code>, IDE workspace scope, etc.) and you&#39;ve added <code>.clawterm-worktrees</code> to every relevant tool&#39;s ignore list.</p><h2 id="maintainer-reference-file-line" tabindex="-1">Maintainer reference (file:line) <a class="header-anchor" href="#maintainer-reference-file-line" aria-label="Permalink to &quot;Maintainer reference (file:line)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Concern</th><th>File</th><th>Range</th></tr></thead><tbody><tr><td>Path resolver (three modes, edge cases)</td><td><code>src/worktree-base.ts</code></td><td>full file</td></tr><tr><td>Frontend orchestration (entry points, dialog wiring, IPC sequence)</td><td><code>src/worktree-actions.ts</code></td><td>full file</td></tr><tr><td>Branch-picker dialog UI</td><td><code>src/worktree-dialog.ts</code></td><td>full file</td></tr><tr><td>Rust git-worktree handlers</td><td><code>src-tauri/src/worktree.rs</code></td><td>full file</td></tr><tr><td>Inside-worktree detection</td><td><code>src-tauri/src/worktree.rs</code></td><td><code>is_inside_worktree</code>, <code>:84</code></td></tr><tr><td><code>find_repo_root</code> (uses <code>--git-common-dir</code>)</td><td><code>src-tauri/src/worktree.rs</code></td><td><code>:271</code></td></tr><tr><td>Resolver unit tests</td><td><code>tests/worktree-base.test.ts</code></td><td>full file (16 cases)</td></tr><tr><td>Rust worktree tests</td><td><code>src-tauri/src/worktree.rs</code></td><td><code>mod tests</code>, end of file</td></tr><tr><td>Config defaults</td><td><code>src/config.ts</code></td><td><code>:122</code></td></tr><tr><td>Config schema</td><td><code>src/config-types.ts</code></td><td><code>:74</code></td></tr><tr><td>Session persistence shape</td><td><code>src/session.ts</code></td><td><code>:4-37</code></td></tr><tr><td>Session restore wire-up</td><td><code>src/terminal-manager.ts</code></td><td><code>restoreOneTab</code></td></tr><tr><td>Legacy hint</td><td><code>src/terminal-manager.ts</code></td><td><code>maybeShowLegacyWorktreeHint</code>, <code>:312</code></td></tr><tr><td><code>has_legacy_in_repo_worktrees</code> Rust check</td><td><code>src-tauri/src/main.rs</code></td><td><code>:208</code></td></tr></tbody></table><h2 id="related-issues" tabindex="-1">Related issues <a class="header-anchor" href="#related-issues" aria-label="Permalink to &quot;Related issues&quot;">​</a></h2><ul><li><a href="https://github.com/clawterm/clawterm/issues/233" target="_blank" rel="noreferrer">#233</a> — Original multi-branch multi-agent workspace feature</li><li><a href="https://github.com/clawterm/clawterm/issues/235" target="_blank" rel="noreferrer">#235</a> — Worktree metadata lost on session restore</li><li><a href="https://github.com/clawterm/clawterm/issues/351" target="_blank" rel="noreferrer">#351</a> — <code>find_repo_root</code> returning the worktree path; introduced <code>--git-common-dir</code></li><li><a href="https://github.com/clawterm/clawterm/issues/415" target="_blank" rel="noreferrer">#415</a> — Worktrees inside main repo broke parent-repo tools (the bug)</li><li><a href="https://github.com/clawterm/clawterm/issues/416" target="_blank" rel="noreferrer">#416</a> — Worktree relocation: implementation plan (the fix)</li></ul>`,77)])])}const k=t(s,[["render",i]]);export{u as __pageData,k as default};
